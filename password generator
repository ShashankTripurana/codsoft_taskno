import string as st
import random as rn
a=int(input("enter the lenght of password: "))
str=st.ascii_letters+st.punctuation+st.digits
password=''
for i in range(a):
    b=rn.choice(str)
    password+=b
print(password)
